name: cartera-clara

on:
  push:
    paths: ["cartera-clara/**", ".github/workflows/cartera-clara.yml"]
  pull_request:
    paths: ["cartera-clara/**", ".github/workflows/cartera-clara.yml"]

permissions:
  contents: read

jobs:
  calidad:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cartera-clara
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy
      - run: uv run pytest -m "not red and not llm and not declarenta"
